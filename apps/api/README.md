# @scarce-pay/api

Backend API built with Hono.

## Development

```bash
pnpm dev      # Start dev server on port 3001 (tsx watch)
pnpm build    # Compile TypeScript
pnpm start    # Run compiled JS
```

## Endpoints

- `GET /api/health` - Health check
